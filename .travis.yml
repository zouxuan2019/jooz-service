language: java
jobs:
  include:
    - stage: build
      script: mvn compile
    - stage: test
      script: mvn test
    - stage: deploy
      deploy:
      script: mvn clean package -DskipTests
      provider: azure_web_apps
      on:
      branch: master
